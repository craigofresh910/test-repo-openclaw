- Twilio voice setup on ducksurfer server: /var/www/ducksurfer/apps/api/src/routes/twilio.ts used for /api/twilio/voice (PM2 app ducksurfer-api). Verified caller ID +1 313-502-3958 added for Twilio calls.
- Found MiniMax key invalid (API returns invalid api key). Claude key from Da vault lacks model access; Anthropic API returns model not found for several Claude model names.
- Updated twilio route to add call-level memory (per CallSid, 10 turns, 1h TTL) and gather settings; wrote Twilio request debug logs to /tmp/twilio-debug.log.
- Need new MiniMax API key; user said updated in Da vault but Notes access via memo only shows 2 notes and cannot find Da vault on this machine.
- Twilio voice production app confirmed at /var/www/ducksurfer/apps/api (PM2 ducksurfer-api). Twilio number +1 313-329-7704 webhook https://ducksurfer.com/api/twilio/voice; STT working (SpeechResult in /tmp/twilio-debug.log).
- Local-only LLM path implemented via Ollama (llama3.2:3b) at http://127.0.0.1:11434/api/chat; env vars OLLAMA_SYSTEM_PROMPT/OLLAMA_MODEL/OLLAMA_TEMPERATURE in /var/www/ducksurfer/apps/api/.env.
- ElevenLabs TTS integrated: /api/twilio/tts/:id serves cached audio (5 min TTL) and TwiML now uses <Play> with fallback <Say>; env vars ELEVENLABS_API_KEY/VOICE_ID/MODEL plus TWILIO_PUBLIC_BASE_URL=https://ducksurfer.com and TWILIO_TTS_TTL_MS.
- ElevenLabs voice clone IDs: original QyuhiqQE7myIeZcKNAHK (active), v2 from IMG_3258 audio s6zJryrDnJeD4HOjw6FY (tested, sounded worse, reverted).
- User reminder: all accessible repos are under https://github.com/craigofresh910/.
